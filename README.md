# Notes → Markdown Ingestion Engine

A local-first ingestion pipeline that converts heterogeneous note formats (TXT, PDF, DOCX, etc.) into clean, structured Markdown optimized for Obsidian knowledge graphs.

## Features

- Recursive folder ingestion
- Multi-format extraction (TXT, PDF, DOCX, MD)
- Structured Markdown generation
- Obsidian-compatible output
- Designed for AI-assisted cognition pipelines

## Usage

```bash
python main.py <input_folder> --out <obsidian_vault>
# knowledge-cortex
